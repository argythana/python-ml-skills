[project]
name = "skill-data-connector"
version = "0.1.0"
description = "Data connector skill - inspect and connect to data sources"
requires-python = ">=3.12"
dependencies = [
    "ml-skills-core",
]

[tool.uv.sources]
ml-skills-core = { workspace = true }

[project.scripts]
data-connect = "skill_data_connector.connect:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
