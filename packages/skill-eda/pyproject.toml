[project]
name = "skill-eda"
version = "0.1.0"
description = "Exploratory Data Analysis skill for tabular data"
requires-python = ">=3.12"
dependencies = [
    "ml-skills-core",
]

[tool.uv.sources]
ml-skills-core = { workspace = true }

[project.scripts]
eda-column-dist = "skill_eda.column_dist:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
